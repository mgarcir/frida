Frida templates.

This repo contains three python template to use with Frida.

Contains a pass.c file for testing puporses too.

In case Frida can attach to the the process, maybe you must run the script as root or execute:

echo 0 > /proc/sys/kernel/randomize_va_space 
